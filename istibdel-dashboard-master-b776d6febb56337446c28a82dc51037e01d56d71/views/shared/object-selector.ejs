<div ><button class="btn btn-default category-selector-btn">
 {{getLabel()}}<img ng-if="!getLabel()" src="{{selectedObject}}" style="width:20px;height:20px;" ng-click="selectObject(obj)"/> <i class="fa fa-angle-down"></i></button>
    <div class="webui-popover-content">
        <ul class="dropdown-menu">
            <li ng-repeat="obj in objects">
                <a ng-hide="icon" href="javascript:void(0);" ng-click="selectObject(obj)">
                    {{getLabel(obj)}}</a>
                <!-- <img src="{{obj.file.url()}}" style="width:40px;height:40px;" ng-click="selectObject(obj)"/>
             --></li>
        </ul>
    </div>
</div>